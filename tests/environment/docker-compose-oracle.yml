services:
  database:
    image: alfresco/oracle-database:${ORACLE_TAG}
    mem_limit: 2g
    environment:
      - ORACLE_SID=ORCL
      - ORACLE_PDB=PDB1
      - ORACLE_PWD=alfresco
      - ORACLE_CHARACTERSET=UTF8
    command: exec $ORACLE_BASE/$RUN_FILE
    ports:
      - 1521:1521
